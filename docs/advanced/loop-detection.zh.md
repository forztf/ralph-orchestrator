# 循环检测

## 概述

Ralph Orchestrator 包含自动循环检测功能,用于防止代理陷入重复循环。该功能使用模糊字符串匹配来比较最近的代理输出,并检测代理是否在重复产生相似的响应。

## 工作原理

`SafetyGuard` 类维护一个包含最近 5 次代理输出的滑动窗口。每次成功迭代后,当前输出会使用 [rapidfuzz](https://github.com/rapidfuzz/RapidFuzz) 这个快速模糊字符串匹配库与历史记录进行比较。

### 检测算法

1. 每次成功迭代后,捕获代理的输出
2. 将输出与存储的最近 5 次输出进行比较
3. 如果任何比较超过 90% 的相似度阈值,则检测到循环
4. 将当前输出添加到历史记录中(如果已满则删除最旧的)
5. 检测到循环时,编排器会记录警告并退出

#### 滑动窗口可视化

```
                                         🔄 滑动窗口 (deque maxlen=5)

┌────────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐  被移除    ╭───╮
│ 新输出     │ ──> │ 输出 5   │ ──> │ 输出 4   │ ──> │ 输出 3   │ ──> │ 输出 2   │ ──> │ 输出 1   │ ─────────> │ X │
└────────────┘     └──────────┘     └──────────┘     └──────────┘     └──────────┘     └──────────┘            ╰───╯
```

<details>
<summary>graph-easy 源码</summary>

```
graph { label: "🔄 滑动窗口 (deque maxlen=5)"; flow: east; }
[ 新输出 ] -> [ 输出 5 ] -> [ 输出 4 ] -> [ 输出 3 ] -> [ 输出 2 ] -> [ 输出 1 ]
[ 输出 1 ] -- 被移除 --> [ X ] { shape: rounded; }
```

</details>

### 相似度阈值

默认阈值是 **90% 相似度**(0.9 比率)。这是基于行业最佳实践选择的:

- **0.95**: 太严格 - 只能检测到几乎相同的输出
- **0.90**: 平衡 - 检测重复模式同时允许变化(推荐)
- **0.85**: 宽松 - 误报率较高

#### 决策流程

```
              🔍 循环检测决策

                         ╭────────────────────╮
                         │   当前输出         │
                         ╰────────────────────╯
                           │
                           │
                           ∨
                         ┌────────────────────┐
                         │ 与历史记录比较     │ <┐
                         └────────────────────┘  │
                           │                     │
                           │                     │
                           ∨                     │
╔═══════════════╗  是    ┌────────────────────┐  │
║ 检测到循环    ║ <───── │   比率 >= 90%?     │  │ 是
╚═══════════════╝        └────────────────────┘  │
                           │                     │
                           │ 否                  │
                           ∨                     │
                         ┌────────────────────┐  │
                         │   更多输出?        │ ─┘
                         └────────────────────┘
                           │
                           │ 否
                           ∨
                         ┌────────────────────┐
                         │   添加到历史记录   │
                         └────────────────────┘
                           │
                           │
                           ∨
                         ╭────────────────────╮
                         │      继续          │
                         ╰────────────────────╯
```

<details>
<summary>graph-easy 源码</summary>

```
graph { label: "🔍 循环检测决策"; flow: south; }
[ 当前输出 ] { shape: rounded; } -> [ 与历史记录比较 ]
[ 与历史记录比较 ] -> [ 比率 >= 90%? ]
[ 比率 >= 90%? ] -- 是 --> [ 检测到循环 ] { border: double; }
[ 比率 >= 90%? ] -- 否 --> [ 更多输出? ]
[ 更多输出? ] -- 是 --> [ 与历史记录比较 ]
[ 更多输出? ] -- 否 --> [ 添加到历史记录 ]
[ 添加到历史记录 ] -> [ 继续 ] { shape: rounded; }
```

</details>

## 示例

```python
# 循环检测内部工作原理示例

from rapidfuzz import fuzz

# 来自迭代 1-3 的代理输出
outputs = [
    "让我检查数据库中的用户信息...",
    "我将查询数据库以查找用户数据...",
    "正在检查数据库中的用户信息...",  # 与 #1 相似
]

# 相似度检查
ratio = fuzz.ratio(outputs[0], outputs[2]) / 100.0
# 结果: ~0.91 (91% 相似) - 检测到循环
```

## 配置

目前,循环检测使用固定参数:

| 参数             | 值   | 描述                           |
| ---------------- | ---- | ------------------------------ |
| `loop_threshold` | 0.9  | 相似度阈值(90%)                |
| `recent_outputs` | 5    | 要比较的输出数量               |

未来版本可能会将这些作为配置选项公开。

## 与其他安全特性的交互

循环检测与其他安全机制协同工作:

1. **迭代限制**: 最大迭代次数(默认: 100)
2. **运行时限制**: 最长时间(默认: 4 小时)
3. **成本限制**: 最大成本(默认: $10)
4. **连续失败限制**: 最大连续失败次数(默认: 5)
5. **循环检测**: 基于相似度的输出比较

当**任何**这些条件满足时,编排器将退出。

### 集成架构

下图显示循环检测如何与主编排循环集成:

```
            ⚙️ 编排循环中的 SafetyGuard

                               ╭─────────────────────╮
  ┌──────────────────────────> │   开始迭代          │ <┐
  │                            ╰─────────────────────╯  │
  │                              │                      │
  │                              │                      │
  │                              ∨                      │
  │                            ┌─────────────────────┐  │
  │                            │ SafetyGuard.check() │  │
  │                            └─────────────────────┘  │
  │                              │                      │
  │                              │                      │
  │                              ∨                      │
  │  ╔════════════════╗  否    ┌─────────────────────┐  │
  │  ║  停止: 限制    ║ <───── │     限制正常?       │  │
  │  ╚════════════════╝        └─────────────────────┘  │
  │                              │                      │
  │                              │ 是                   │
  │                              ∨                      │
  │                            ┌─────────────────────┐  │
  │                            │  检查完成度         │  │
  │                            └─────────────────────┘  │
  │                              │                      │
  │                              │                      │
  │                              ∨                      │
  │  ╔════════════════╗  是    ┌─────────────────────┐  │
  │  ║   停止: 完成   ║ <───── │   任务已完成?       │  │ 否
  │  ╚════════════════╝        └─────────────────────┘  │
  │                              │                      │
  └────┐                         │ 否                   │
       │                         ∨                      │
       │                       ┌─────────────────────┐  │
       │                       │    执行代理         │  │
       │                       └─────────────────────┘  │
       │                         │                      │
       │                         │                      │
       │                         ∨                      │
     ┌────────────────┐  否    ┌─────────────────────┐  │
     │ 处理失败       │ <───── │      成功?          │  │
     └────────────────┘        └─────────────────────┘  │
                                 │                      │
                                 │ 是                   │
                                 ∨                      │
                               ┌─────────────────────┐  │
                               │    detect_loop()    │  │
                               └─────────────────────┘  │
                                 │                      │
                                 │                      │
                                 ∨                      │
                               ┌─────────────────────┐  │
                               │     发现循环?       │ ─┘
                               └─────────────────────┘
                                 │
                                 │ 是
                                 ∨
                               ╔═════════════════════╗
                               ║     停止: 循环      ║
                               ╚═════════════════════╝
```

<details>
<summary>graph-easy 源码</summary>

```
graph { label: "⚙️ 编排循环中的 SafetyGuard"; flow: south; }
[ 开始迭代 ] { shape: rounded; } -> [ SafetyGuard.check() ]
[ SafetyGuard.check() ] -> [ 限制正常? ]
[ 限制正常? ] -- 否 --> [ 停止: 限制 ] { border: double; }
[ 限制正常? ] -- 是 --> [ 检查完成度 ]
[ 检查完成度 ] -> [ 任务已完成? ]
[ 任务已完成? ] -- 是 --> [ 停止: 完成 ] { border: double; }
[ 任务已完成? ] -- 否 --> [ 执行代理 ]
[ 执行代理 ] -> [ 成功? ]
[ 成功? ] -- 否 --> [ 处理失败 ]
[ 处理失败 ] -> [ 开始迭代 ]
[ 成功? ] -- 是 --> [ detect_loop() ]
[ detect_loop() ] -> [ 发现循环? ]
[ 发现循环? ] -- 是 --> [ 停止: 循环 ] { border: double; }
[ 发现循环? ] -- 否 --> [ 开始迭代 ]
```

</details>

## 循环检测触发场景

循环检测在以下场景中有帮助:

- **代理卡在同一任务上**: 反复尝试相同的操作
- **振荡**: 代理在两种相似方法之间切换
- **API 错误**: 一致的重试消息
- **占位符响应**: 代理返回类似的"正在处理"消息

## 日志记录

触发循环检测时,您将看到:

```
WARNING - 检测到循环: 与之前输出的相似度为 92.3%
WARNING - 由于重复的代理输出而中断循环
```

## 重置循环检测

循环检测历史记录在以下情况下自动清除:

- 新的编排会话开始
- 调用 `SafetyGuard.reset()`
- 编排器完成(成功或失败)

## 依赖项

循环检测需要 `rapidfuzz` 包:

```bash
pip install "rapidfuzz>=3.0.0,<4.0.0"
```

如果未安装 rapidfuzz,循环检测将被优雅地跳过并输出调试日志消息。

## 最佳实践

1. **监控循环**: 在日志中观察循环检测警告
2. **改进提示词**: 如果循环频繁发生,请优化您的任务描述
3. **检查任务完整性**: 确保任务有明确的完成标准
4. **使用完成标记**: 完成时添加 `- [x] TASK_COMPLETE`

## 相关主题

- [安全机制](../guide/overview.md#safety-features)
- [故障排除](../troubleshooting.md)
- [术语表: 循环检测](../glossary.md#l)
